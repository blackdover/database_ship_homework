# 🚀 港口集装箱运营系统 - 启动步骤清单

## ✅ 前置检查

- [ ] Python 3.8+ 已安装
- [ ] MySQL 5.7+ 或 8.0+ 已安装并运行
- [ ] pip 已安装

---

## 📋 启动步骤

### 步骤 1️⃣：进入项目目录

```bash
cd container_management
```

---

### 步骤 2️⃣：安装 Python 依赖

```bash
pip install django mysqlclient simpleui
```

**⚠️ Windows 用户注意：**

如果 `mysqlclient` 安装失败，使用 `pymysql` 替代：

```bash
pip install django pymysql simpleui
```

然后在 `container_management/container_management/__init__.py` 文件开头添加：

```python
import pymysql
pymysql.install_as_MySQLdb()
```

---

### 步骤 3️⃣：创建数据库和表

**方法一：使用完整初始化脚本（推荐，会自动创建数据库）**

```bash
# 在项目根目录执行（不是 container_management 目录）
cd ..
mysql -u root -p < init_database.sql
```

**方法二：手动创建数据库后导入表结构**

```bash
mysql -u root -p
```

在 MySQL 中执行：

```sql
CREATE DATABASE box_management CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
USE box_management;
SOURCE createtable.sql;
```

或者直接导入：

```bash
mysql -u root -p box_management < createtable.sql
```

**方法三：如果表已存在，只需创建视图**

```bash
mysql -u root -p box_management < views.sql
```

---

### 步骤 4️⃣：配置数据库连接

编辑文件：`container_management/container_management/settings.py`

找到 `DATABASES` 配置，修改为你的数据库信息：

```python
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': 'box_management',        # 数据库名（通常不需要改）
        'USER': 'root',                  # 你的 MySQL 用户名
        'PASSWORD': 'iamshp0228',        # 你的 MySQL 密码（修改这里！）
        'HOST': 'localhost',
        'PORT': '3306',
    }
}
```

**⚠️ 重要：** 将 `PASSWORD` 改为你的 MySQL root 密码！

---

### 步骤 5️⃣：创建超级用户（管理员账号）

```bash
cd container_management
python manage.py createsuperuser
```

按提示输入：
- 用户名（Username）
- 邮箱（Email，可选）
- 密码（Password，输入两次）

**示例：**
```
Username: admin
Email: admin@example.com
Password: ********
Password (again): ********
```

---

### 步骤 6️⃣：启动开发服务器

```bash
python manage.py runserver
```

看到以下输出表示启动成功：

```
Starting development server at http://127.0.0.1:8000/
Quit the server with CTRL-BREAK.
```

---

### 步骤 7️⃣：访问系统

打开浏览器，访问：

**🔗 http://localhost:8000/admin/**

使用步骤 5 创建的超级用户账号登录。

---

## 🎯 功能页面访问

登录后，在 Admin 首页可以看到四个功能入口：

1. **📦 堆场库存** - `/admin/yard-inventory/`
2. **🚢 船舶访问** - `/admin/vessel-visit/`
3. **📋 任务管理** - `/admin/task-management/`
4. **📊 统计报表** - `/admin/statistics/`

---

## 🔧 常见问题排查

### ❌ 问题 1：数据库连接失败

**错误信息：** `django.db.utils.OperationalError: (2003, "Can't connect to MySQL server")`

**解决方法：**
1. 检查 MySQL 服务是否运行
   ```bash
   # Windows
   服务管理器 → 找到 MySQL → 启动
   
   # 或命令行
   net start MySQL
   ```
2. 检查数据库配置是否正确（用户名、密码、数据库名）
3. 检查 MySQL 是否允许本地连接

---

### ❌ 问题 2：表不存在错误

**错误信息：** `Table 'box_management.xxx' doesn't exist`

**解决方法：**
1. 确认已执行 `init_database.sql` 或 `createtable.sql`
2. 检查数据库是否存在：
   ```sql
   SHOW DATABASES;
   USE box_management;
   SHOW TABLES;
   ```
3. 如果表不存在，重新执行建表脚本

---

### ❌ 问题 3：视图不存在错误

**错误信息：** `View 'box_management.View_XXX' doesn't exist`

**解决方法：**
```bash
mysql -u root -p box_management < views.sql
```

---

### ❌ 问题 4：mysqlclient 安装失败（Windows）

**错误信息：** `Microsoft Visual C++ 14.0 is required`

**解决方法：**
使用 `pymysql` 替代（见步骤 2 的 Windows 用户注意）

---

### ❌ 问题 5：端口被占用

**错误信息：** `Error: That port is already in use.`

**解决方法：**
1. 使用其他端口：
   ```bash
   python manage.py runserver 8001
   ```
2. 或关闭占用 8000 端口的程序

---

## 📝 快速启动命令（一键执行）

如果你已经配置好数据库，可以快速启动：

```bash
# 1. 进入项目目录
cd container_management

# 2. 启动服务器
python manage.py runserver
```

然后访问：http://localhost:8000/admin/

---

## 🎓 测试数据（可选）

如果需要测试数据，可以在 MySQL 中执行：

```sql
USE box_management;

-- 插入测试港口
INSERT INTO Port_Master (Port_Name, Port_Code, Country) VALUES
('上海港', 'CNSHA', 'CN'),
('深圳港', 'CNSZN', 'CN'),
('青岛港', 'CNTAO', 'CN');

-- 插入测试相关方
INSERT INTO Party (Party_Name, Party_Type, City, Country) VALUES
('测试船公司A', 'COMPANY', '上海', 'CN'),
('测试船公司B', 'COMPANY', '深圳', 'CN');

-- 更多测试数据可以根据需要添加
```

---

## ✅ 启动成功标志

- ✅ 服务器启动，显示 `Starting development server at http://127.0.0.1:8000/`
- ✅ 浏览器能访问 `http://localhost:8000/admin/`
- ✅ 能使用超级用户登录
- ✅ 能看到四个功能入口（堆场库存、船舶访问、任务管理、统计报表）
- ✅ 功能页面能正常显示数据（即使没有数据，页面也应该能正常加载）

---

## 📞 需要帮助？

如果遇到问题，检查：
1. Python 版本：`python --version`（需要 3.8+）
2. MySQL 版本：`mysql --version`（需要 5.7+）
3. Django 版本：`python manage.py --version`
4. 数据库连接：在 MySQL 中测试连接
5. 查看 Django 错误日志（终端输出的错误信息）

---

**🎉 祝使用愉快！**



